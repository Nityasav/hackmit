# dependencies / build
node_modules/
.next/
out/
__pycache__/
*.pyc
.venv/
.pytest_cache/
.ruff_cache/

# env
.env
.env*.local

# local data
api/data/
# Generated record packs. These are outputs of fixtures/generate_saas.py, not source,
# and the truth file beside them is an answer key that must never reach the app.
generated/
*.db
*.sqlite3

# local tooling output
.superpowers/
.playwright-mcp/
.DS_Store
schooltrace/package-lock.json
bun.lockb

# macOS/iCloud duplicates ("file 2.ts"). They shadow real files and break
# typecheck, because lint and tsconfig exceptions target the real names.
* [0-9].*

# bun is the package manager (see web/package.json "packageManager")
package-lock.json

# build artifact from `pip install -e api`
*.egg-info/
